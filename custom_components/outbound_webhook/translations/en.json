{
  "config": {
    "step": {
      "user": {
        "title": "Add Outbound Webhook Rule",
        "description": "Configure a new outbound webhook rule to trigger on entity or attribute changes."
      },
      "options": {
        "title": "Edit Outbound Webhook Rule",
        "description": "Update the settings for your outbound webhook rule."
      }
    },
    "field": {
      "name": {
        "name": "Name"
      },
      "entity_id": {
        "name": "Entity"
      },
      "trigger_type": {
        "name": "Watch",
        "options": {
          "state": "State Change",
          "attribute": "Attribute Change"
        }
      },
      "attribute": {
        "name": "Attribute"
      },
      "webhook_url": {
        "name": "Webhook URL"
      },
      "http_method": {
        "name": "HTTP Method",
        "options": {
          "POST": "POST",
          "GET": "GET",
          "PUT": "PUT",
          "DELETE": "DELETE"
        }
      },
      "rate_limit_mode": {
        "name": "Rate Limit Mode",
        "options": {
          "none": "None",
          "throttle": "Throttle (send first, then suppress)",
          "debounce": "Debounce (wait for stability)"
        }
      },
      "rate_limit_interval": {
        "name": "Rate Limit Interval (ms)"
      },
      "enabled": {
        "name": "Enabled"
      }
    }
  },
  "error": {
    "cannot_connect": "Cannot connect to the webhook endpoint.",
    "invalid_url": "Invalid webhook URL.",
    "invalid_interval": "Invalid rate limit interval."
  }
}
